# Python
__pycache__/
*.py[cod]
*.pyo
venv/
.env

# Checkpoints & large data (track by manifest, not by content)
data/checkpoints/
data/datasets/
*.pth
*.pt
*.ckpt
*.h5

# Serving-comparison shard artifacts (DAT-SERVING-01): regenerable from
# data/checkpoints/ by scripts/build_shard_manifest.py, same size class as
# the checkpoints themselves -- not tracked by content, same rule as above.
data/serving_manifests/

# Raw results (keep in git — immutable record)
# results/raw/ is intentionally NOT ignored

# Figures (regenerated from scripts)
figures/*.png
figures/*.pdf

# Paper build artifacts
paper/*.aux
paper/*.log
paper/*.bbl
paper/*.blg
paper/*.out
paper/*.synctex.gz

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

data/cifar10/
