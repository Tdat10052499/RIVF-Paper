# Action items for Dat — 26 September 2026

**From:** Chinh  
**Deadline:** end of 26/09, before the team compile pass  
**Branch:** pull `main` first — Chinh's 09:12 commit (`a25d19ee`) changes the paper structure,
class options, and references.bib. Your local copy is stale if you have not pulled.

---

## Context

Chinh's commit this morning fixed the document class (`[conference,a4paper]` +
`\pdfminorversion=6`), cleaned `references.bib` (dropped all note fields, fixed authors and
venues, added 5 new refs — 16 total), and rewrote Chinh's own sections.

Your sections — Deployment / Serving Comparison, and the EDAS submission logistics — still
have four items that need fixing before the draft goes to Dr. Hung.

---

## Item T1 — Serving section: state the hardware

The serving experiment section does not state what hardware the latency numbers were measured
on. RIVF reviewers will ask. Add one sentence at the start of the section:

> "All serving-latency measurements were taken on a single NVIDIA \textless GPU model\textgreater{}
> with \textless RAM\textgreater{} GB RAM, running PyTorch \textless version\textgreater{}."

Fill in the actual values from your experiment card (`notes/2026-09-21_dat_serving_experiment_card.md`).

---

## Item T2 — Serving section: n=3 and HMAC wording

Two specific fixes in the same section:

**n=3:** The current text does not state the number of repetitions used to average latency.
Add "averaged over n=3 forward passes" (or whatever n you used; check your notebook).

**HMAC wording:** The current text describes HMAC as if it is the production signing
mechanism. We implemented it as a stand-in. Change to:

> "…using HMAC-SHA256 as a keyed-hash stand-in for a production model-signing scheme
> such as Sigstore~\cite{sigstore} or OpenSSF…"

(The Sigstore reference is not yet in `references.bib` — add it, or use the placeholder
`\cite{tuf}` and note in the text that TUF uses the same primitive.)

---

## Item T3 — Serving section: remove "pending 23/09 freeze"

The table caption (or a nearby paragraph) contains a comment about the 23/09 experiment
freeze, e.g. "pending freeze" or "results as of 23/09". Remove it entirely.
The freeze is an internal process note; it must not appear in the submitted paper.

Search `paper/main.tex` for `freeze` and `23/09` and `pending` and delete those phrases.

---

## Item T4 — EDAS and authorship (coordinate with Dr. Hung)

Two questions that need answers before you set up the EDAS submission:

1. **Double-blind?** Check whether RIVF 2026 SS3 ("Recent Advances in 6G Communications")
   uses double-blind review. If it does, the author list must be removed from the submitted
   PDF, and any self-identifying language ("our previous work", "our GitHub", lab-name
   acknowledgements) must be anonymised. The class option `[conference,a4paper]` does not
   suppress author names by default in IEEEtran — you may need `[conference,a4paper,anonymous]`
   if the track requires it.

2. **Dr. Hung as author?** The supervisor note in `notes/2026-09-18_hung_supervisor.md`
   is ambiguous about whether he wants to be listed. Ask him directly before setting up
   the EDAS submission form. EDAS author order is final once the paper is registered.

---

## Item T5 — Broken cross-reference `??` (sec:secondseed → sec:seed)

If you see `??` anywhere in a compiled PDF, it is because `\ref{sec:secondseed}` is used
somewhere but the label defined in `main.tex` is `\label{sec:seed}`. Search for
`sec:secondseed` and replace with `sec:seed`.

Chinh's commit may have already resolved this — compile and check before spending time on it.

---

## Checklist before pushing

- [ ] Serving hardware stated (GPU model, RAM, PyTorch version)
- [ ] "averaged over n=3" (or correct n) added to serving section
- [ ] HMAC described as "keyed-hash stand-in"; Sigstore or TUF cite added
- [ ] "pending freeze" / "23/09" phrases removed from table caption and body
- [ ] Confirmed with Dr. Hung: double-blind requirement and authorship
- [ ] EDAS submission form set up (author list, title, abstract, track = SS3)
- [ ] No `??` in compiled PDF

Push to `main` (or your branch and open a PR) and ping Chinh when done.
Chinh will do the team compile pass once all items are in.
