# Daily Note — 2026-09-16 (Duong Ngoc Linh Dan)

## What I did today
- Set up Python venv on Windows (PyTorch 2.14.0+cpu, torchvision 0.29.0+cpu)
- Pulled infected and repaired checkpoints from team SharePoint / Chính's push
- Wrote `src/partition/shard.py` with three functions:
  - `get_layer_prefixes`: extracts top-level layer group names from a state dict
  - `partition`: splits a state dict into N contiguous shards by layer prefix
  - `substitute`: replaces one shard in the repaired model with the same shard from the infected model

## Results
- Verified model has 6 layer groups: `['conv1', 'layer1', 'layer2', 'layer3', 'layer4', 'linear']`
- With n_shards=3: Shard 0 = 25 keys, Shard 1 = 50 keys, Shard 2 = 27 keys (total 102)
- Substitute test confirmed shard 0 keys match infected model after swap ✓

## Machine
- Dell / Windows 11, CPU-only
- Python 3.11, PyTorch 2.14.0+cpu

## What remains uncertain
- Whether n_shards=3 is the right default or should be configurable per experiment
- Need to integrate substitute() into the experiment script that measures ASR after swap

## Tomorrow's smallest decisive experiment
- Write `scripts/run_substitution.py`: load both checkpoints, substitute shard 0, run inference on bd_test, print ASR
- Owner: Dan / Second reader: Chinh or Dat
