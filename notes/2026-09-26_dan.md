# Action items for Dan — 26 September 2026

**From:** Chinh  
**Deadline:** end of 26/09, before the team compile pass  
**Branch:** pull `main` first — Chinh's 09:12 commit (`a25d19ee`) changes the paper structure significantly.

---

## Context

Chinh's commit this morning rewrote the abstract, threat model, setup, metrics, discussion,
limitations, related work, and conclusion. The ANP section is now gated behind `\ifincludeanp`
(currently OFF). The paper compiles to exactly 6 A4 pages with the gate off.

Your sections — Introduction, Single-Shard Rollback, Shard-Selection Strategy Comparison,
Sanity Checks — still carry four factual errors and one wrong attribution that need to be
fixed before the draft can go to Dr. Hung.

---

## Item D1 — Introduction: remove the seed and ANP claims

**Current text (approximate):**
> "…replicates exactly across two independent random seeds…"
> "…ANP's utility collapse…" (or similar, describing the two-regime result)

**Why it is wrong:**
- Repair-aware selection is deterministic: running it twice with the same seed gives the same
  result by definition. There is no independent replication; it is the same computation run
  twice. Saying "two independent random seeds" is false.
- The ANP model used for the two-regime result was trained on a completely different BadNets
  base (cosine similarity ≈ 0.02 at conv1 vs. our infected model). All ANP numbers in the
  current paper are invalid. Chinh's commit removed ANP from results; the Introduction must
  not reference the old result either.

**Fix:**
- Delete or replace the "replicates exactly across two independent random seeds" sentence.
  The new subsection "Determinism and Sampling Variance" (behind `\ifrepairseed`) handles
  this correctly once the second seed run is done.
- Delete any sentence in the Introduction that describes ANP collapse, two-regime results,
  or utility drop under ANP. If you want a forward-reference to ANP, use the placeholder
  already in `\ifincludeanp` block — leave it empty for now.

---

## Item D2 — Single-Shard Rollback (Fig. 1): shards 1 and 4 are no longer "n.t."

**Current state:** Fig. 1 shows shards 1 and 4 as "n.t." (not tested).

**Reality:** The data exists. From `results/raw/substitution/n6/`:

| Shard | ASR   | CA     |
|-------|-------|--------|
| 1     | 1.29% | 90.95% |
| 4     | 8.08% | 90.52% |

Chinh's `make_locality_pgf.py` already regenerated `paper/fig_locality_data.tex` with all
six shards. **You only need to remove the "n.t." text from the body/caption** — the figure
data is already correct.

Search for `n\.t\.` in `paper/main.tex` and remove or replace every occurrence in the
single-shard rollback section. Also update any sentence that says "four shards tested"
to "six shards tested" (or "all six shards").

---

## Item D3 — Strategy comparison: "strictly most efficient" is wrong

**Current claim:** Repair-aware selection is strictly the most efficient strategy.

**Why it is wrong:** Subset {2, 4} at k=2 achieves ASR = 74.76%. Repair-aware at k=2
selects {3, 4} and achieves only 4.57%. So repair-aware is *not* the most efficient at k=2;
a different pair dominates it.

**Fix:** Change to "best of the three ordered strategies" (random, greedy-share, repair-aware),
and add one sentence acknowledging that exhaustive search over all C(6,2)=15 pairs can do
better. The all-subset eval (Chinh, §5 of fix guide) will produce the full data;
Dan can add the {2,4} number once Chinh pushes `results/raw/all_subsets/finetune_s42_n6.json`.

Exact wording suggestion:
> "Among the three ordered selection strategies, repair-aware selection achieves the lowest
> ASR at each k. Exhaustive search over all C(n,k) subsets can exceed this bound;
> for example, subset \{2,4\} reaches ASR = 74.76\% at k=2 vs.\ 4.57\% for repair-aware."

---

## Item D4 — Sanity Checks / full-rollback table: CA range is wrong

**Current claim:** "hybrid CA between 84.58% and 93.26%" (or similar).

**Reality:** The evaluated hybrids range from **73.55% to 93.26%** CA. The lower bound is
wrong. Check `results/raw/substitution/n6/` for the full table.

Also check: "invisible to utility monitoring" and "CA above 86% in all hybrids" — both are
contradicted by 73.55%. Replace with the correct range.

---

## Item D5 — Attribution: "our re-trained checkpoints"

**Current text:** Somewhere in the Baseline Reproduction paragraph the infected model is
described as having been "retrained" or "trained by us".

**Reality:** The infected checkpoint (`resnet18_cifar10_badnets_infected.pt`) is the
published BackdoorBench pretrained model, downloaded from their release. We did not train it.

**Fix:** Replace with "the published BackdoorBench checkpoint for PreActResNet18 / CIFAR-10
/ BadNets (poison rate 0.10) \cite{backdoorbench2024}". Chinh's Setup section already says
this correctly; align your wording with it.

---

## Checklist before pushing

- [ ] "two independent random seeds" sentence removed from Introduction
- [ ] ANP collapse / two-regime reference removed from Introduction
- [ ] Fig. 1 "n.t." text gone; body says "six shards"
- [ ] Strategy comparison says "best of three ordered strategies"; {2,4} example added
- [ ] CA range corrected to 73.55–93.26%
- [ ] "retrained" → "published BackdoorBench checkpoint"

Push to `main` (or your branch and open a PR) and ping Chinh when done.
Chinh will do the team compile pass once all items are in.
